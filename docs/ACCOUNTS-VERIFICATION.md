# 계정 전환 검증 — 2026-09-17

- 전체 `npm test`: 종료코드 0.
- 격리 Tabby 계정 화면 검사: 14개 통과. Claude/Codex 필터, 비밀번호 비노출, 테마 닫기 버튼,
  하단 화면과 같은 사용률 막대(`ctx` 제외), Esc 닫기, 진행 중 상태에서 계정 선택,
  저장된 인증으로 새 터미널 탭 생성 및 실제 Codex 프로세스 시작을 확인.
- 기존 메타데이터 표시 검사: 12개 통과, 실패 0, 생략 0.
- Windows DPAPI 인증정보 암호화·복원·새로 갱신된 인증정보 보존 검사 통과.
- 실제 설정된 Claude 2개와 Codex 2개 계정에서 인증정보 저장 및 사용량 조회 확인.
- Codex `model_usage`는 Astra의 사용 가능 여부만 제공하여 퍼센트를 만들어 표시하지 않음.
- 별도 임시 git 저장소에서 계정 파일에 값이 있으면 커밋 검사 실패, 빈 파일은 통과함을 확인.
- `git diff --check`: 통과. 커밋·푸시는 하지 않음.

검증한 격리 빌드와 동일한 번들을 실사용 경로에 반영한 뒤 자동 핫리로드를 확인했다.
실사용 진단 로그(`%USERPROFILE%\.agentdeck-diag.log`):

- `2026-09-17T10:18:07.073Z reload go reason=dev:build tabs=4`
- `2026-09-17T10:18:11.081Z reload restored tabs=4 dead=0 ms=1921`

기존 4개 세션을 모두 유지했다. 실제 계정 파일은 저장소 밖에 있으며,
커밋용 파일은 `examples/accounts.example.json`의 빈 템플릿이다.
