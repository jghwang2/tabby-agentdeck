# AgentDeck 개발 지침

작업 전에 `docs/DEVELOPMENT.md`의 개발 정책과 `docs/REGRESSION.md`를 읽는다.

실사용 앱이 감시하는 경로에 먼저 빌드하지 않는다. 별도 staging 빌드 → 격리 앱 동작·회귀 검증 → 성공한 동일 산출물 반영 → 핫리로드 순서를 지킨다. 단위 테스트·타입 검사·빌드 성공은 격리 앱 검증을 대체하지 않는다. 상세 기준의 단일 출처는 `docs/DEVELOPMENT.md`의 **실사용 핫리로드 전 필수 게이트**다.

실패나 환경 미충족을 보고하는 것으로 끝내지 않는다. 수정·재검증을 반복하고, 통과하면 핫리로드와 세션 복구 확인까지 자율적으로 완료한다.
